FIREBASE_PROJECT_ID=your-project-id-here
SERVICE_ACCOUNT_PATH=../service-account.json
SERVER_PORT=8080
ALLOWED_ORIGIN=http://localhost:5173

# Optional deployment settings (process environment overrides this file).
SERVER_HOST=127.0.0.1
UPLOAD_DIRECTORY=../uploads
# Leave blank for direct/local access. Never trust arbitrary forwarded headers.
TRUSTED_PROXY_IP=
